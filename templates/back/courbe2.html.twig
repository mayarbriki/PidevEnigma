<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<canvas id="myChart2" width="400" height="400"></canvas>

<script>
	var ctx = document.getElementById("myChart2");
var myChart2 = new Chart(ctx, {
type: "polarArea",
data: {
labels: {{ produit|json_encode|raw }},
datasets: [
{
label: "TND",
data: {{ somme|json_encode|raw }},
fill: false,
borderColor: "rgba(0, 5, 26, 1)",
tension: 0.1
}
]
},
options: {
scales: {
y: {
ticks: {
beginAtZero: true
}
}
},
plugins: {
tooltip: {
enabled: true,
mode: 'index',
intersect: true,
callbacks: {
title: function (tooltipItems) { // Display the label of the hovered item as tooltip title
return tooltipItems[0].label;
},
label: function (tooltipItem, data) { // Display the value of the hovered item as tooltip label
return tooltipItem.formattedValue + ' produit(s)';
}
}
}
}
}
});
</script>
