<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

<canvas id="myChart3" width="400" height="400"></canvas>

<script>
	var ctx = document.getElementById("myChart3");
var myChart3 = new Chart(ctx, {
type: "bar",
data: {
labels: {{ email|json_encode|raw }},
datasets: [
{
label: "TND",
label: "Total des commandes par utilisateur",
data: {{ somme2|json_encode|raw }},
fill: false,
borderColor: "rgba(0, 178, 26, 1)",
tension: 0.3
}
]
},
options: {
scales: {
y: {
ticks: {
beginAtZero: true
}
}
},
plugins: {
tooltip: {
enabled: true,
mode: 'index',
intersect: true,
callbacks: {
title: function (tooltipItems) { // Display the label of the hovered item as tooltip title
return tooltipItems[0].label;
},
label: function (tooltipItem, data) { // Display the value of the hovered item as tooltip label
return tooltipItem.formattedValue + 'TND';
}
}
}
}
}
});
</script>
